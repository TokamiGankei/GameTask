<UserControl x:Class="GameTaskPlugin.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">

    <StackPanel Margin="20">

        <TextBlock Text="GameTask Settings"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <!-- Bring to Foreground -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <CheckBox IsChecked="{Binding Settings.BringWindowToForeground}"
                      VerticalAlignment="Center"/>
            <TextBlock Text="Bring game window to foreground after launch"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0"/>
            <TextBlock Text="?"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0"
                       Foreground="Gray"
                       FontWeight="Bold"
                       Cursor="Help">
                <TextBlock.ToolTip>
                    <ToolTip>
                        <TextBlock MaxWidth="320" TextWrapping="Wrap"
                            Text="When enabled, GameTask waits for the game process to appear and automatically brings its window to the foreground. Recommended for Playnite fullscreen mode, where games may open behind the Playnite window.&#x0a;&#x0a;After changing this setting, run 'Repair All Tagged Games' so the launchers are regenerated." />
                    </ToolTip>
                </TextBlock.ToolTip>
            </TextBlock>
        </StackPanel>

        <!-- Detect Orphan Tasks -->
        <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
            <CheckBox IsChecked="{Binding Settings.DetectOrphanTasks}"
                      VerticalAlignment="Center"/>
            <TextBlock Text="Detect orphan tasks on startup"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0"/>
            <TextBlock Text="?"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0"
                       Foreground="Gray"
                       FontWeight="Bold"
                       Cursor="Help">
                <TextBlock.ToolTip>
                    <ToolTip>
                        <TextBlock MaxWidth="320" TextWrapping="Wrap"
                            Text="When enabled, GameTask checks the Windows Task Scheduler on every Playnite startup and notifies you if it finds tasks that no longer match any game in your library (e.g. games removed without using 'Disable GameTask').&#x0a;&#x0a;You can also trigger this check manually via Extensions → GameTask → Clean Orphan Tasks." />
                    </ToolTip>
                </TextBlock.ToolTip>
            </TextBlock>
        </StackPanel>

        <!-- Detect Corrupted Tasks -->
        <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
            <CheckBox IsChecked="{Binding Settings.DetectCorruptedTasks}"
                      VerticalAlignment="Center"/>
            <TextBlock Text="Detect corrupted tasks on startup"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0"/>
            <TextBlock Text="?"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0"
                       Foreground="Gray"
                       FontWeight="Bold"
                       Cursor="Help">
                <TextBlock.ToolTip>
                    <ToolTip>
                        <TextBlock MaxWidth="320" TextWrapping="Wrap"
                            Text="When enabled, GameTask checks whether the executable path registered in each scheduled task still exists on disk. If a game was moved or uninstalled without disabling GameTask first, the task becomes corrupted.&#x0a;&#x0a;A notification will appear for each affected game with a link to fix the executable path." />
                    </ToolTip>
                </TextBlock.ToolTip>
            </TextBlock>
        </StackPanel>

        <Separator Margin="0,16,0,12"/>

        <TextBlock TextWrapping="Wrap" Foreground="Gray" FontSize="11"
                   Text="Changes are saved automatically when you click OK. Some settings (e.g. 'Bring game window to foreground') require running 'Repair All Tagged Games' to take effect on existing games." />

    </StackPanel>
</UserControl>
